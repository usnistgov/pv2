import "./NotFound.sass";

const NotFound = () => {
    return <div className={".not-found"}>
        <h1>Requested resource was not found.</h1>
        <p>Please try another page.</p>
    </div>
};

export default NotFound;
